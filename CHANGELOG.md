# 更新日志

版本规则（正式上线前）：
- 不使用 1.x.x，全部以 0.x.x 起步
- patch（0.x.P）：bug 修复、小调整
- minor（0.M.0）：功能新增、较大改动

## 0.15.5

邀请入口重设计（实装方案 A）。

- 邀请成员从底部大按钮**移至名册首位**：虚线圆头像「＋」+ 品牌色「邀请成员」文字，与成员行同构——微信群成员列表的原生模式，用户零学习成本
- 点击行为不变：直接拉起微信分享面板
- 底部只保留治理操作（归档/解散/退出），职责分区更清晰：名册区=人，底部区=管理

成员 Tab 动作栈对齐（设计讨论定稿：邀请留底部、与治理按钮统一节奏）。

- 邀请/归档/解散/退出四按钮统一高度 96rpx、统一 16rpx 间距节奏，形成清晰动作栈：绿=邀请（建设）、琥珀=归档（中危）、红=解散（高危）
- 邀请按钮与成员行之间 24rpx 呼吸间距

## 0.15.3

邀请按钮视觉重构（真机反馈）。

- 邀请按钮从「虚线灰底 + 白色加号（浅底上不可见）」升级为**品牌渐变主按钮**，与登录/提交/生成按钮同一视觉语言
- 新增专用分享图标（三节点网络 shareWhite），按钮语义一目了然
- 文案精简为「邀请微信好友加入」；玻璃模式下自动获得毛玻璃质感变体

角色动作流程全量走查修复（7 项，含 2 个高危死路）。

- 🔴 **认领后待办不可见**：`getMyTodos` 等四处统计只按 `assigneeId` 过滤，认领池认领后任务永远不出现在首页；新增 `isMyTodo` 双通道判定（assigneeId 或 assignments 含我），四处过滤统一替换
- 🔴 **认领池误挂创建者**：`createTodo` 认领模式把 `assigneeId` 填成创建者自己，未认领的池子错误出现在创建者首页；认领模式创建后 assignee 置空（与种子数据一致）
- 🔴 **分享进入未登录死路**：访客点「加入」只弹 toast 无路可走；改为引导弹窗 → 登录页携带回跳地址，登录后 `redirectTo` 回到团队详情继续加入
- 🟡 **登录跳转加固**：`ensureLogin` 从 Tab 页发起 `redirectTo` 不可靠，改 `reLaunch`；登录成功回跳区分 Tab 页（switchTab）与普通页（redirectTo）
- 🟡 **零团队建待办死路**：创建页无团队时显示引导卡「去创建团队」，建团返回自动刷新团队列表（补 onShow）
- 🟡 **非成员可见邀请按钮**：已退出/未加入用户不再显示分享邀请入口
- 🟢 搜索无结果空态文案（「未找到匹配的待办」）；模板库空团队给「新建团队」行动按钮
- 冒烟测试扩至 **22 组**（认领可见性回归 ×2）

## 0.15.2

角色动作流程全量走查修复（7 项，含 2 个高危死路）。

## 0.15.1

真机走查修复：邀请动线、治理操作层级、按钮排版、建队入口。

- **邀请动线重构**：「邀请成员」提示框删除，成员 Tab 底部按钮改为原生 `open-type="share"`，点击直接拉起微信分享面板选好友——不再需要引导文案里描述的「右上角转发」
- **治理操作醒目化**：归档（琥珀描边）/ 解散（红色描边）/ 退出（中性）从灰色文字链接升级为三个独立全宽按钮，中高危操作一目了然
- **按钮文字居中修复**：登录按钮、待办详情「标记为已完成」等全部原生 button 从 `line-height` 居中改为 flex 居中 + `padding:0`，根治真机文字偏下
- **新建团队入口显性化**：团队列表底部新增虚线大按钮「＋ 新建团队」；空团队时空态直接给「新建团队」行动按钮（原先只有左上角小入口，真机不可见）

体验与增长：品牌分享封面 + 准实时协作。

- **Canvas 品牌分享封面**：团队详情页预生成分享图（团队标识色渐变底 + 头像圆 + 团队名 + 成员/待办数 + 邀请文案），转发到群里的卡片从「默认截图」升级为品牌物料；生成失败自动回退默认截图
- **准实时协作**：团队详情/待办详情页前台停留时每 30 秒轻量云同步（本地模式自动跳过省电），队友的完成、认领、评论不用下拉刷新也能看到；onHide/onUnload 自动停止轮询

## 0.14.0

场景模板库：一键生成整组待办，冷启动与效率双杀。

- **四套场景模板**：课程作业（选题→资料→初稿→定稿）、活动筹备（方案→物料→宣传→执行→复盘）、值班排班（5 个工作日认领制）、内容排期（选题→初稿→排版→发布）
- **templates 页**：模板卡展开配置面板——关联团队、开始日期（任务按 offset 顺延自动预览日期）、指派成员多选（认领制模板自动跳过）
- **一键生成**：批量 createTodo 入队，每条独立进动态流；完成后自动返回首页
- 入口：首页列表头新增模板按钮（宫格图标），与搜索并排
- 新增 grid 图标；store 导出 getDateStrOffset 供日期顺延计算

## 0.13.0

协作治理完善：角色管理 + 团队解散 + 同步可视化 + 反馈上云。

### 团队治理增强

- **角色管理**：创建者点选成员弹出操作面板——设为管理员/撤销管理员/移出团队；管理员身份获得与创建者相同的移除成员、编辑删除待办权限（创建者受保护不可操作）
- **解散团队**：创建者专属，二次确认后级联软删除团队 + 全部成员行 + 队内全部待办（墓碑随同步下发，全员端一致消失）

### 同步可视化

- 首页问候卡新增**同步状态芯片**：已同步（绿）/ 同步中（琥珀）/ 同步异常（红）/ 本地模式（灰），数据链路状态一眼可见
- 「我的」页新增数据模式信息行（云端同步 / 本地存储）
- **墓碑自动清理**：每次成功同步后清除本地超过 30 天的软删除行，防止长期使用后本地存储无限膨胀

### 反馈上云

- 新增 feedbacks 表（migration 0002，RLS：登录可写、仅见自己）；帮助页反馈在登录态下直接上行云端，本地留底兜底
- FAQ 更新数据安全说明（云同步已上线）

### 其他

- 首页新增**待办搜索**：列表头搜索按钮展开标题关键词过滤，与时间/状态筛选叠加生效
- 冒烟测试扩至 **20 组**（角色管理权限、解散级联、墓碑清理）

## 0.12.0

真实可用版：待办编辑 + 资料设置 + 云同步关键修复（Supabase 对接后的第一个功能版本）。

### 🔴 修复潜在集成炸弹（联调前置）

- **同步字段映射**：云端列名 snake_case（avatar_char）vs 本地实体 camelCase（avatarChar），原引擎直接原样上行必然全量报错；新增六表双向字段映射层（toRemote/toLocal），push/pull 全部经过转换，`_dirty` 本地标记不上行

### 待办编辑（P0 功能洞补齐）

- `store.updateTodo(id, patch)`：创建者/管理员可改标题、描述、截止日期+时间、优先级、重复规则；字段规范化（非法日期清空、空标题拒绝）
- create-todo 页升级双模式：`?id=` 进入编辑，标题变「编辑待办」，团队与成员指派锁定展示（含「不可更改」标记与说明）
- 入口：待办详情「编辑」链接 + 首页/团队页长按菜单新增「编辑待办」

### 个人资料设置

- 新页 profile-edit：昵称 + 头像标识色 + 实时预览
- `store.updateUserProfile` 三处联动：登录态、**全部团队成员行**（同 id 多团队一次改齐）、云端 profiles 行打脏随同步上行
- 入口：「我的」页用户卡点击（带箭头提示）

### 401 会话自动刷新

- `api.withAuthRetry`：REST 调用遇 401 自动用 refresh_token 换新会话并重试一次（单飞行去重防并发刷新风暴）
- 刷新失败清会话抛错，同步静默降级，下次登录自动恢复

### 其他

- 云端登录失败自动降级本地登录（域名白名单配置前不受阻）
- profiles 集合纳入同步表清单与完整性校验；云端登录后自动上行个人资料行
- 冒烟测试扩至 **17 组**（编辑权限/字段、资料联动传播、字段映射往返无损）

- 冒烟测试扩至 **17 组**（编辑权限/字段、资料联动传播、字段映射往返无损）

## 0.11.2

玻璃感根治 + 铃铛层级修复（第二轮截图走查）。

- **铃铛被导航栏遮挡**：铃铛 z-index(20) 低于导航栏(fixed z:100)，几何居中后整个圆钮藏进了导航玻璃面之下；提升至 z:200 悬浮于导航之上
- **玻璃感缺失的根因**：卡片背后是近白页面底色，58% 白 + 模糊(白) 在数学上仍是白。三管齐下：
  - 卡面透明度 0.58→0.48（次级面 0.40→0.34）
  - 环境光大幅加浓：光斑同心双叠层模拟双倍饱和度、新增中部/底部品牌色渐升底光（::after 层）、覆盖高度延伸至 96vh
  - 模糊升级为 `blur + saturate(160%)`，透过卡片看到的颜色更鲜活

  - 模糊升级为 `blur + saturate(160%)`，透过卡片看到的颜色更鲜活

## 0.11.1

首页玻璃细节修复（截图走查反馈）。

- **消息铃铛修正**：此前灰色细线图标叠在浅色环境光上几乎隐形、垂直位置偏低垂到卡片上方，看起来像个「空泡泡」极易误触；改为品牌色铃铛图标（新增 bellBrand）+ 圆钮中心与系统胶囊几何居中（按 rpx→px 实际换算）
- **FAB 存在感恢复**：新建按钮从半透明品牌 tint 改回实色品牌渐变糖果质感 + 光晕投影，不再透出底下卡片
- **统计卡浮起感加强**：阴影从 sm 升至卡片级 md + 高光内描边

## 0.11.0

玻璃质感第二轮强化 + 经典底栏加固（真机观感反馈迭代）。

### 经典模式底栏修复

- **内容垫底重叠**：TabBar 改为自然高度后实际约 112rpx，页面预留仍是 100rpx，底部文字与底栏上沿重叠；`--tabbar-height` 令牌对齐至 112rpx 根治
- **透明兜底**：底栏背景加字面色 fallback（`background: #ffffff` + `var(--bg-primary, #ffffff)` 双声明）+ `z-index: 10`，任何变量解析异常都不会透明

### 液态玻璃观感强化

- **TabBar 胶囊**：改为上浅下深渐变填充（不再与背景同色），边框提亮至 0.70 透明度白、投影加深至 0.22、饱和度 180%；暗色胶囊独立渐变；选中项药丸底带品牌光晕
- **环境光增强**：柔光从 3 团增至 4 团（新增品牌色中层光斑），覆盖面积扩大，毛玻璃透色更明显
- **覆盖补齐**：团队列表卡片/搜索框、团队动态卡、关于/帮助页卡片全部接入表面材质；创建待办分段控件选中态改白色浮层；首页铃铛升级为悬浮玻璃圆钮；filter-tabs 容器在玻璃下去底色

## 0.10.1

液态玻璃效果强化 + TabBar 图标溢出修复（真机反馈迭代）。

- **修复 TabBar 图标溢出**：底栏原为固定 100rpx 高 + 安全区 padding 内缩，在有 Home Indicator 的设备上内容区仅剩 ~30rpx 导致图标顶出胶囊；改为内容撑开高度（经典模式同样受益），彻底解决
- **TabBar 悬浮重构**：玻璃模式胶囊脱离屏幕下缘（外边距含安全区），加深透明度 + `saturate(160%)` 提升通透感，选中项品牌色药丸底，隐藏独立安全条
- **环境光背景**：玻璃模式页面顶部铺三团柔光（品牌色/信息色/暖色 radial-gradient，z-index 负层），毛玻璃从此「有东西可透」，白底通透感问题根治；暗色模式自动适配
- **全站悬浮分层**：15 类卡片浮起（阴影加深 + 顶部高光内描边）、range-tabs 筛选胶囊本体玻璃化、stat-card/搜索框描边高光、todo-card/filter-tabs 组件消费阴影令牌
- **主按钮糖果质感**：提交/登录/标记完成按钮升级为品牌渐变 + 光晕投影 + 内高光，FAB 光晕加强

## 0.10.0

液态玻璃主题：新增「界面风格」维度，经典扁平之外可选 iOS 风格毛玻璃质感。

- **材质令牌体系**：新增 `--surface / --surface-2 / --surface-blur / --surface-edge` 表面材质令牌（经典模式=实色/无模糊，渲染零变化），27 处卡片容器全部切换为令牌引用；CSS 变量穿透组件样式隔离，todo-card/filter-tabs 等组件无需传参即可换材质
- **液态玻璃风格**：`.style-glass` 变量块——半透明卡片面 + `backdrop-filter` 毛玻璃 + 白色高光描边 + 内阴影；暗色玻璃独立配色（深板岩半透明）；导航栏同步降透明度
- **浮动胶囊 TabBar**：玻璃模式下 TabBar 脱离底边悬浮，胶囊圆角 + 毛玻璃背景 + 高光边框
- **FAB 玻璃化**：首页新建按钮切换为品牌 tint 半透明胶囊 + 模糊 + 高光边
- **profile 界面风格面板**：经典 / 液态玻璃二选一，与三款皮肤 × 暗色模式自由组合（共 3 皮肤 × 2 风格 × 2 模式 = 12 种外观组合），持久化存储
- 兼容性：backdrop-filter 在 iOS 全支持、安卓 XWeb 内核支持；老设备自动降级为半透明纯色面（无模糊），不影响可读性

## 0.9.0

体验完善版：建队闭环 + 主题换肤 + 交互/视觉打磨（产品全面评估后的定向迭代）。

### 功能断链修复（P0）

- **创建团队**：新增 create-team 页面（名称/简介/标识色选择 + 实时预览），`store.createTeam` 建团与创建者入队一步完成，打通新用户冷启动死路
- **首页铃铛避让胶囊**：位置改为按 `menuButton` 矩形精确计算，对齐到胶囊左侧，不再与微信菜单热区重叠
- **分享直达页返回兜底**：team-detail 归档/退出、todo-detail 删除后的返回均加 switchTab 兜底，分享单页栈场景不再被困

### 主题换肤落地（P1）

- 新增 `utils/themes.js` 皮肤定义；三款皮肤：翡翠绿（默认）/ 海洋蓝 / 落日橙
- app.wxss 扩展 `.skin-ocean/.skin-sunset` 变量块（light+dark 双套）；`getThemeClass()` 输出暗色×皮肤组合类
- profile「主题换肤」从灰态变为真实面板：色板点选即时全局生效并持久化
- TabBar 选中图标颜色运行时注入（SVG 模板 + `__COLOR__` 占位符 + 当前皮肤品牌色重编码），随换肤即时变化

### 色值令牌化（P1，换肤前置）

- 新增语义令牌 `--brand-tint / --brand-tint-strong / --warning-tint / --error-tint / --info-tint`（含暗色变体）
- 清理 7 个文件共 16 处硬编码色值（rgba 品牌淡底、#ef4444/#f59e0b/#3b82f6 实色）全部替换为令牌引用

### 交互修复与打磨（P1/P2）

- 下拉刷新接入云同步：home/team-list/team-detail 刷新时先 `syncNow()` 再渲染
- 首页环形进度：问候卡完成率升级为 SVG 环形图，颜色跟随皮肤与暗色模式
- 首页逾期警示条：有逾期待办时显示红条一键直达逾期视图
- 待办卡长按快捷菜单：催办未完成成员 / 删除待办，免进详情页
- 分享加入弹窗时序修复：由不可靠的 setTimeout 改为 loadData 数据就绪后触发
- 列表卡片按压反馈：todo-card / team-item / 换肤选项统一 hover 态
- 首页筛选状态记忆：时间维度与状态维度选择持久化，回访不重置
- 认领通知发起人：认领池被抢后发起人收到定向消息
- 消息中心已读时机优化：停留 1.5s 后标记已读，离开兜底落盘

## 0.8.0

0.5-0.8 四个主题合并交付：后端地基 + 提醒闭环 + 协作深度 + 团队治理。

### 后端地基（Supabase，0.5）

- 双轨架构：`utils/config.js` 填入 Supabase 项目信息即切云端模式；未配置自动运行本地模式，功能完整可演示
- `utils/api.js`：基于 wx.request 的 Supabase REST 客户端（PostgREST 数据表 / GoTrue 认证 / Edge Functions），零 SDK 依赖
- `utils/sync.js`：离线优先同步引擎——本地缓存始终是读写入口，后台 push 脏行 upsert + pull 按 updated_at 游标增量拉取，LWW 冲突策略，软删除墓碑随行下发
- 真实登录：wx.login → Edge Function（supabase/functions/wx-login）code2session 换 openid → magiclink token 换 Supabase 会话；本地模式降级为模拟登录
- 数据库：supabase/migrations/0001_init.sql 六张表（profiles/teams/members/todos/comments/events）+ updated_at 触发器 + RLS 行级权限
- 到期提醒推送：supabase/functions/remind-cron 扫描 24h 内到期待办发微信订阅消息（pg_cron 定时触发）
- 接入指南：supabase/README.md（建库、部署函数、订阅消息模板、域名白名单全流程）

### 提醒闭环（0.6，配得上「remind」这个名字）

- 待办支持**截止时间**（日期 + 可选 HH:mm 时间选择器）
- 创建待办成功后自动拉起订阅消息授权（云端模式 + 已配模板时生效）
- **催办**：待办详情页「催一下」一键提醒所有未完成成员，生成定向通知
- **消息中心**新页：催办/提及等定向通知列表，首页铃铛入口带未读角标，「我的」页同步显示未读数

### 协作深度（0.7）

- **评论**：待办详情评论区，@成员名 自动识别为提及并通知对方（@ 按钮从团队成员快速选人）
- **认领池**：创建待办可选「认领池」模式，发布 N 个空名额团队成员抢单；详情页/卡片显示空位与认领按钮
- **重复任务**：每天/每周规则，完成后自动生成下一期（同内容、日期顺延、完成态清零）
- **团队动态流**：团队详情新增「动态」Tab，创建/完成/认领/评论/加入全记录，可跳转对应待办

### 团队治理（0.8）

- 角色权限体系：creator / admin / member 三级，管理员可移除成员（创建者受保护）、删除任意待办
- 成员管理：成员行内移除按钮、退出团队（创建者暂不可退出，引导归档）
- **团队归档**：创建者可归档团队（列表隐藏、随时恢复），团队列表底部折叠分组展示已归档团队
- **团队周报**：近 7 天新建/完成率/逾期存量概览、每日新建趋势图、成员贡献排行（按已完成排序）

### 其他

- 种子数据新增认领池示例待办；全部实体补齐 updatedAt/deleted 同步字段
- 新增图标：clock（时间选择）；ESLint 忽略 supabase/ 目录

## 0.4.1

持久化层加固 + 数据一致性全量修复：

- P0 修复 `reset()` 未做日期占位符解析的 Bug：重置后 `__TODAY__` 字面量导致排序/逾期判断失效；reset 与 init 共用同一条播种管线，行为完全一致
- P0 统一身份体系：用户 id 即成员 id（同一人多团队共享同 id），移除全部「按姓名匹配身份」的兜底逻辑（同名成员不再错乱）；种子数据、assignments、createdBy 全部对齐
- P0 持久化层加固：所有 Storage 读写包异常保护（配额满/损坏不再抛未捕获错误）；新增内存缓存写穿透层，消除高频重复读盘；启动时校验集合完整性，任一集合损坏自动重新播种
- P0 存储结构版本号（schemaVersion）：旧结构数据升级时自动迁移重置，避免新旧字段模型混用
- P1 id 生成改为 时间戳+自增序列+随机数，杜绝同毫秒撞车
- P1 种子数据一致性：t2/t3 成员表补齐实际记录（16 条成员记录），memberCount 与详情页真实一致；张明同时是产品设计组创建者与运营推广组成员
- P1 收敛三处重复的 memberId 解析逻辑为 `store.findMyAssignment(todo)`；完成/取消完成的 toast 文案按操作结果区分
- P1 登录 loading 状态在成功跳转前复位，不再依赖页面跳走掩盖；auth.js 异常路径可真正 reject
- P2 首页 RANGE_DEFS 取 label 增加空值保护；createTodo 对 dueDate 做 YYYY-MM-DD 规范化
- 工程化：CI 接入 ESLint 质量门禁 + 提交 package-lock.json 改用 `npm ci` 可复现构建；移除 CI 日志中的私钥头行打印

## 0.4.0

多人协作 + 待办详情 + 团队邀请：

- 待办详情页：新增独立页面 `todo-detail`，点击首页/团队详情任意待办卡进入；展示标题/描述/优先级/截止/状态、所属团队入口、整体进度条与完成率、团队内每个成员的完成状态
- 多人指派数据模型：待办由单 assignee 改为 `assignments: [{ memberId, memberName, avatarChar, avatarColor, done }]`；`decorate()` 计算 `assignTotal/assignDone/assignRate`；`toggleAssignment(todoId, memberId)` 切换单人完成状态并自动同步整单状态（全完成→已完成，否则→进行中）
- 创建待办支持多选成员：create-todo 改为多选 chip 列表，默认选中当前用户，提交时传 `selectedMembers` 数组由 store 生成 assignments
- todo-card 多人进度展示：多人指派的待办卡片右下角显示 N/M 进度徽标，全员完成时高亮品牌色；副标题由「X 指派」改为「N 人指派」
- 团队邀请（微信分享）：team-detail 接入 `onShareAppMessage`，分享卡片带 `?from=share` 参数；对方打开后弹窗确认加入，调用 `joinTeamByShare` 自动加入团队并同步 memberCount
- 邀请入口：成员 Tab 底部「邀请成员」按钮改为可用态，点击弹窗引导使用右上角转发/分享按钮
- 数据层：store 新增 `getTodoById / toggleAssignment / addMember / joinTeamByShare`；mock.js 6 条种子待办全部接入 assignments 数组

## 0.3.0

首页体验升级 + 待办优先级 + 入口去重：

- 首页顶部新增问候卡：按时段问候（早上好/下午好…）+ 今日日期 + 今日完成进度条（N/M + 百分比），打开即知今天状态
- 首页新增时间维度视图切换：今日 / 本周 / 全部 三段式 Tab，默认聚焦「今日」，符合「今天做什么」心智
- 首页统计卡升级：已完成卡显示今日完成率（百分比），其余卡保持数量；与时间维度、状态维度形成双重筛选
- 待办优先级：create-todo 新增紧急/普通选择；todo-card 紧急项标题左侧红点标识；store.createTodo 支持 priority 字段
- 种子数据动态化：mock.js 待办日期用占位符，init 时替换为相对今天的真实日期，确保演示永远贴近当前时间；新增逾期样例
- 设置页去重：移除「我的团队」入口（团队归 Tab 2 独立管理），消息通知归入偏好设置组
- 数据层：store 新增 getTodayStats / getMyTodosByRange / getGreeting / getTodayLabel / getDateStrOffset

## 0.2.0

UIUX 框架重构首版 + 全站统一收口，分两批处理评估出的 P0/P1/P2 问题：

- P1 设计令牌补齐：新增间距令牌（space-xs~2xl）、字号令牌（fs-xs~2xl）、暗色模式变量集（.theme-dark）
- P1 抽出 5 个公共组件：state-view（loading/error/empty 三态）、todo-card、filter-tabs、empty-state、avatar，全站替换
- P1 Tab 结构 4→3：首页吸收待办，统计卡同时作为筛选条件，点击联动下方列表；移除独立待办 Tab
- P1 TabBar 改用 SVG 图标，修复 CSS 伪元素绘制图标在真机异常问题
- P1 内容区 / FAB 安全区 calc 修正
- P0 设置页「假按钮」统一处理：深色模式真生效；主题换肤/消息通知/帮助反馈标注「即将上线」灰态；关于读真实版本号
- P0 team-detail 团队为空时接入 state-view，修复整页空白
- P0 create-todo 提交按钮加 loading + disabled，防重复提交
- P0 移除「AI 感」：team-detail 渐变 wash、profile 顶部渐变条、卡片左边色条统一收口；login logo 渐变光晕、FAB 品牌色发光阴影、团队卡/待办卡左侧色条全部移除；shadow-elevated 改为中性高度阴影
- P1 CSS 手绘图标统一换 SVG：team-list（search/clear/chevron）、login（check-mark）、create-todo（calendar/chevron）全部替换为 utils/icons 中的 base64 SVG；新增 plusBrand 图标
- P1 themeClass 全站级联：9 个页面根容器接入；新增 --nav-bg token，nav-bar 随主题切换
- P1 关键修复：TabBar 是页面同级组件，CSS 变量不会从 .page-container 级联进来 → 给 TabBar 加 updateTheme() 方法，三个 Tab 页 onShow + profile 切换时即时同步深色模式
- P1 组件接入：team-list 接入 avatar + empty-state；create-todo 接入 SVG 图标
- P0 profile 设置页全部功能打通：深色模式真实切换+持久化+TabBar同步、我的团队跳转、帮助与反馈、关于（真实版本号）、用户协议、隐私政策（原生全文页）
- P2 接入 utils/date.js 相对时间格式化（今天/明天/N天后/已逾期）全站替换原始 ISO
- P2 全站下拉刷新校验：home/team-list/team-detail 启用，create-todo 表单页显式关闭；关键操作触觉反馈统一
- P2 暗色模式真正实现：变量集 + 根类切换 + 持久化
- 工程化：gen-icons.js / gen-tabbar.js 脚本同步更新，支持 plusBrand 与 TabBar 主题方法
